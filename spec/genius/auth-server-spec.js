'use babel'

// import server from '../../lib/genius/auth-server'

describe('Genius OAuth server', () => {
  describe('routes', () => {
    describe('genius/auth', () => {
      it('should redirect you to the Genius authentication page', () => {

      })
    })
  })
})
